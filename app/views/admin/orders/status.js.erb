$('.status').text("<%= @order.status %>");
if ('<%= @order.status %>' === 'paid' ) {
  $('.status-button').remove();
  $('.status-buttons').html("<%=j link_to 'Mark as Completed', admin_order_status_path(@order.id, 'completed'), method: :PATCH, remote: true, class:'btn btn-grey3 status-button' %>");
} else if ('<%= @order.status %>' === 'completed') {
  $('.status-button').remove();
}
$('.updated').text('<%= (@order.updated_at).to_formatted_s(:long) %>');
