<h2>Menu</h2>
<h3>Categories</h3>
<% @categories.each do |category| %>
<%= link_to category.title, category_path(category) %></td>
<% end %>
<h3>Menu Items</h3>
<%= render 'layouts/display_table'  %>
