<div id = "categories">
<ul id='category_list'>
<h3>Categories</h3>
<% @categories.each do |category| %>
  <li><%= link_to category.title, category, id: category.to_s, remote: true %></li>
<% end %>
</ul>
</div>


<%= render 'shared/display_table'  %>
