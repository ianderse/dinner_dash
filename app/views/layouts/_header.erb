<div class="navbar-wrapper">
  <div class="container">
    <div class="navbar navbar-default navbar-static-top" role="navigation", style="margin-bottom:50px">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= image_tag("caussa_logo_h.png", :alt => "logo", :width => '220', :height => '87' ) %></a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to "Home", root_path %></li>
            <li class="active"><%= link_to "About", about_path %></li>
            <li class="active"><%= link_to "Menu", items_path %></li>
            <li class="active"><%= link_to "Events", items_path %></li>
            <li class="active"><%= link_to "Blog", items_path %></li>
            <li class="active"><%= link_to "Contact", items_path %></li>
          </ul>
          <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
              <% if current_user %>
                <%= "Welcome #{current_user.full_name}!" %>

              <%= link_to "Logout", logout_path, method: :delete %>
              <% else %>
                <%= render 'layouts/login_form'  %>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
<% flash.each do |k, v| %>
  <div class="flash" id="<%= k %>">
    <%= v %>
  </div>
  <% end %>
